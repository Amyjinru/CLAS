# 已弃用的目录服务原型

该目录仅保留历史实现，未纳入 `services/pom.xml`、`scripts/start-services.ps1` 或
`nginx/clas-gateway.conf`。当前唯一受支持的商品目录服务是 `services/clas-catalog`。

请不要部署、修改或以此目录作为服务契约依据；内部目录契约以
`/internal/catalog/v1/**` 和 `docs/三服务接口清单.md` 为准。
