## 1. 联调编排

- [x] 1.1 新增安全的本机联调编排脚本，检查 Docker 与端口并创建带固定前缀的临时依赖。
- [x] 1.2 复用现有数据库初始化、五服务启动和网关主路径冒烟脚本。
- [x] 1.3 在成功和失败路径中仅清理由编排脚本创建的资源。

## 2. 证据留存

- [x] 2.1 将健康、主路径结果和环境版本写入脱敏 Markdown 证据。
- [x] 2.2 更新联调指南，说明共享环境执行方式和 Kubernetes 验收边界。

## 3. 验证

- [x] 3.1 使用 Docker 临时依赖执行一次完整五服务网关联调并保留证据。
- [x] 3.2 验证执行结束后五服务、Nginx 和临时容器均已清理。
- [x] 3.3 分别执行直连与网关主路径冒烟，并在证据中记录两个结果。
