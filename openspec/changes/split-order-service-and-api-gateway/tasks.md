## 1. 依赖冻结与工程基线

- [ ] 1.1 与 #36、#37、#38 负责人冻结多模块目录、端口、服务名、私有 schema、健康检查地址和内部认证方式，并将结论登记到服务接口与表归属文档。
- [ ] 1.2 从现有 `OrderController`、`OrderService`、Mapper、实体、迁移和测试中列出订单领域迁移清单，标注每项当前跨域依赖与替代内部契约。
- [ ] 1.3 建立父 POM、`order-service` 与 `api-gateway` 独立模块及基础配置；确认各模块可单独构建、测试和启动健康检查。
- [ ] 1.4 定义订单、商品目录、商家、支付和配送的内部 HTTP 契约，包括身份、请求 ID、超时、错误码、`eventId`、`orderId` 与版本字段。

## 2. 订单履约服务

- [ ] 2.1 创建订单服务私有 schema/迁移与数据访问层，使 `orders`、`order_item`、`order_lifecycle_event`、`order_refund_dispute` 仅由订单服务写入。
- [ ] 2.2 将订单创建、批量创建、预览、查询、状态机、退款争议和时间线迁入订单服务，保持既有公开 API 响应语义。
- [ ] 2.3 实现商品和商家内部客户端及契约桩；下单时固化商品、价格、商家和配送快照，并移除订单服务的跨服务 Mapper/SQL 访问。
- [ ] 2.4 实现库存确认/补偿、购物车清理、支付和配送状态通知的超时、失败处理、幂等去重与合法状态转换。
- [ ] 2.5 为订单服务增加独立单元和集成测试，覆盖下单、退款、越权、非法状态跳跃、重复事件、商品/商家失败与调用超时。

## 3. 网关与韧性能力

- [ ] 3.1 配置 API 网关通过服务发现或稳定服务名路由订单、商品目录和商家服务，保持 `/api/order/**`、`/api/product/**`、`/api/merchant/**` 对前端兼容。
- [ ] 3.2 实现网关 JWT 校验与透传、`X-Request-Id` 生成/传播、结构化日志和服务版本/健康诊断端点。
- [ ] 3.3 为网关路由与订单内部客户端配置连接/读取超时、限流和熔断或隔离策略，统一返回不泄露内部细节的受控错误。
- [ ] 3.4 编写网关测试，验证三服务路由、有效/无效 JWT、请求 ID 关联、限流、下游超时与服务不可用时的 `503` 响应。

## 4. 容器、部署与回归证据

- [ ] 4.1 为订单服务和网关提供 Dockerfile、Compose 服务、Kubernetes Deployment/Service/Ingress 配置及资源、健康检查和版本化镜像规则。
- [ ] 4.2 扩展 CI/CD，使相关模块变更会构建、测试、生成 SHA 镜像、部署并执行网关路由与健康检查；任一失败阻断发布。
- [ ] 4.3 在可回退环境中执行订单创建/退款主成功、异常授权、非法状态、公开 API 兼容和端到端回归，保存原始报告与日志。
- [ ] 4.4 停止一个依赖服务或注入延迟，保存请求 ID、网关/服务日志、健康检查、用户可见响应和恢复步骤，形成失败排查记录。
- [ ] 4.5 更新微服务划分、接口、表归属、迁移、运行与回退文档；提交 PR 并在 #39、#40 附上提交、CI、测试、部署和故障演示证据。
