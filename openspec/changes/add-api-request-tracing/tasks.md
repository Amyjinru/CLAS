## 1. Backend

- [x] 1.1 Add request trace context and request id normalization.
- [x] 1.2 Add a servlet filter that accepts or generates `X-Request-Id`.
- [x] 1.3 Extend the unified `Result` envelope with `timestamp` and `requestId`.

## 2. Tests and Docs

- [x] 2.1 Add integration tests for success and error envelopes.
- [x] 2.2 Document the updated unified response format.
- [x] 2.3 Run backend tests and frontend build.
- [x] 2.4 Push `dev` and deploy to the cloud server.
