# Apply after namespace.yaml, configmap.yaml, secret creation, mysql.yaml and redis.yaml.
# Replace REQUIRED_TAG with one immutable image tag before applying.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: clas-iam
  namespace: clas
spec:
  replicas: 1
  strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 0, maxUnavailable: 1}}
  selector: {matchLabels: {app: clas-iam}}
  template:
    metadata: {labels: {app: clas-iam}}
    spec:
      # Kubernetes service-link variables such as CLAS_IAM_PORT contain
      # tcp://host:port and collide with Spring's integer port properties.
      enableServiceLinks: false
      imagePullSecrets: [{name: ghcr-pull-secret}]
      containers:
        - name: clas-iam
          image: ghcr.io/amyjinru/clas-iam:REQUIRED_TAG
          ports: [{containerPort: 8081}]
          envFrom: [{configMapRef: {name: clas-config}}, {secretRef: {name: clas-secrets}}]
          env:
            - {name: CLAS_IAM_HOST, value: clas-iam}
            - {name: CLAS_MERCHANT_HOST, value: clas-merchant}
            - {name: CLAS_CATALOG_HOST, value: clas-catalog}
            - {name: CLAS_ORDER_HOST, value: clas-order}
            - {name: CLAS_COMPAT_HOST, value: clas-compat}
          # Keep user avatars in the server's established uploads tree.
          # LocalFileStorage writes /app/uploads/avatars/<userId>/... .
          volumeMounts: [{name: avatars, mountPath: /app/uploads/avatars}]
          startupProbe: {httpGet: {path: /api/health, port: 8081}, periodSeconds: 10, timeoutSeconds: 3, failureThreshold: 30}
          readinessProbe: {httpGet: {path: /api/health, port: 8081}, periodSeconds: 10, timeoutSeconds: 3, failureThreshold: 6}
          livenessProbe: {httpGet: {path: /api/health, port: 8081}, periodSeconds: 20, timeoutSeconds: 3, failureThreshold: 3}
          resources: {requests: {cpu: 150m, memory: 256Mi}, limits: {cpu: 500m, memory: 512Mi}}
      volumes:
        - name: avatars
          hostPath: {path: /opt/clas/backend/uploads/avatars, type: DirectoryOrCreate}
---
apiVersion: v1
kind: Service
metadata: {name: clas-iam, namespace: clas}
spec:
  type: ClusterIP
  selector: {app: clas-iam}
  ports: [{name: http, port: 8081, targetPort: 8081}]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: clas-merchant
  namespace: clas
spec:
  replicas: 1
  strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 0, maxUnavailable: 1}}
  selector: {matchLabels: {app: clas-merchant}}
  template:
    metadata: {labels: {app: clas-merchant}}
    spec:
      enableServiceLinks: false
      imagePullSecrets: [{name: ghcr-pull-secret}]
      containers:
        - name: clas-merchant
          image: ghcr.io/amyjinru/clas-merchant:REQUIRED_TAG
          ports: [{containerPort: 8085}]
          envFrom: [{configMapRef: {name: clas-config}}, {secretRef: {name: clas-secrets}}]
          env:
            - {name: CLAS_IAM_HOST, value: clas-iam}
            - {name: CLAS_MERCHANT_HOST, value: clas-merchant}
            - {name: CLAS_CATALOG_HOST, value: clas-catalog}
            - {name: CLAS_ORDER_HOST, value: clas-order}
            - {name: CLAS_COMPAT_HOST, value: clas-compat}
          # LocalFileStorage writes /app/uploads/merchant-logo/<merchantId>/... .
          volumeMounts: [{name: merchant-logos, mountPath: /app/uploads/merchant-logo}]
          startupProbe: {httpGet: {path: /api/health, port: 8085}, periodSeconds: 10, timeoutSeconds: 3, failureThreshold: 30}
          readinessProbe: {httpGet: {path: /api/health, port: 8085}, periodSeconds: 10, timeoutSeconds: 3, failureThreshold: 6}
          livenessProbe: {httpGet: {path: /api/health, port: 8085}, periodSeconds: 20, timeoutSeconds: 3, failureThreshold: 3}
          resources: {requests: {cpu: 150m, memory: 256Mi}, limits: {cpu: 500m, memory: 512Mi}}
      volumes:
        - name: merchant-logos
          hostPath: {path: /opt/clas/backend/uploads/merchant-logo, type: DirectoryOrCreate}
---
apiVersion: v1
kind: Service
metadata: {name: clas-merchant, namespace: clas}
spec:
  type: ClusterIP
  selector: {app: clas-merchant}
  ports: [{name: http, port: 8085, targetPort: 8085}]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: clas-catalog
  namespace: clas
spec:
  replicas: 1
  strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 0, maxUnavailable: 1}}
  selector: {matchLabels: {app: clas-catalog}}
  template:
    metadata: {labels: {app: clas-catalog}}
    spec:
      enableServiceLinks: false
      imagePullSecrets: [{name: ghcr-pull-secret}]
      containers:
        - name: clas-catalog
          image: ghcr.io/amyjinru/clas-catalog:REQUIRED_TAG
          ports: [{containerPort: 8082}]
          envFrom: [{configMapRef: {name: clas-config}}, {secretRef: {name: clas-secrets}}]
          env:
            - {name: CLAS_IAM_HOST, value: clas-iam}
            - {name: CLAS_MERCHANT_HOST, value: clas-merchant}
            - {name: CLAS_CATALOG_HOST, value: clas-catalog}
            - {name: CLAS_ORDER_HOST, value: clas-order}
            - {name: CLAS_COMPAT_HOST, value: clas-compat}
          # LocalFileStorage writes /app/uploads/product/<merchantId>/... .
          volumeMounts: [{name: product-images, mountPath: /app/uploads/product}]
          startupProbe: {httpGet: {path: /api/health, port: 8082}, periodSeconds: 10, timeoutSeconds: 3, failureThreshold: 30}
          readinessProbe: {httpGet: {path: /api/health, port: 8082}, periodSeconds: 10, timeoutSeconds: 3, failureThreshold: 6}
          livenessProbe: {httpGet: {path: /api/health, port: 8082}, periodSeconds: 20, timeoutSeconds: 3, failureThreshold: 3}
          resources: {requests: {cpu: 150m, memory: 256Mi}, limits: {cpu: 500m, memory: 512Mi}}
      volumes:
        - name: product-images
          hostPath: {path: /opt/clas/backend/uploads/product, type: DirectoryOrCreate}
---
apiVersion: v1
kind: Service
metadata: {name: clas-catalog, namespace: clas}
spec:
  type: ClusterIP
  selector: {app: clas-catalog}
  ports: [{name: http, port: 8082, targetPort: 8082}]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: clas-order
  namespace: clas
spec:
  replicas: 1
  strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 0, maxUnavailable: 1}}
  selector: {matchLabels: {app: clas-order}}
  template:
    metadata: {labels: {app: clas-order}}
    spec:
      enableServiceLinks: false
      imagePullSecrets: [{name: ghcr-pull-secret}]
      containers:
        - name: clas-order
          image: ghcr.io/amyjinru/clas-order:REQUIRED_TAG
          ports: [{containerPort: 8083}]
          envFrom: [{configMapRef: {name: clas-config}}, {secretRef: {name: clas-secrets}}]
          env:
            - {name: CLAS_IAM_HOST, value: clas-iam}
            - {name: CLAS_MERCHANT_HOST, value: clas-merchant}
            - {name: CLAS_CATALOG_HOST, value: clas-catalog}
            - {name: CLAS_ORDER_HOST, value: clas-order}
            - {name: CLAS_COMPAT_HOST, value: clas-compat}
          startupProbe: {httpGet: {path: /api/health, port: 8083}, periodSeconds: 10, timeoutSeconds: 3, failureThreshold: 30}
          readinessProbe: {httpGet: {path: /api/health, port: 8083}, periodSeconds: 10, timeoutSeconds: 3, failureThreshold: 6}
          livenessProbe: {httpGet: {path: /api/health, port: 8083}, periodSeconds: 20, timeoutSeconds: 3, failureThreshold: 3}
          resources: {requests: {cpu: 200m, memory: 384Mi}, limits: {cpu: 750m, memory: 768Mi}}
---
apiVersion: v1
kind: Service
metadata: {name: clas-order, namespace: clas}
spec:
  type: ClusterIP
  selector: {app: clas-order}
  ports: [{name: http, port: 8083, targetPort: 8083}]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: clas-compat
  namespace: clas
spec:
  replicas: 1
  strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 0, maxUnavailable: 1}}
  selector: {matchLabels: {app: clas-compat}}
  template:
    metadata: {labels: {app: clas-compat}}
    spec:
      enableServiceLinks: false
      imagePullSecrets: [{name: ghcr-pull-secret}]
      containers:
        - name: clas-compat
          image: ghcr.io/amyjinru/clas-compat:REQUIRED_TAG
          ports: [{containerPort: 8084}]
          envFrom: [{configMapRef: {name: clas-config}}, {secretRef: {name: clas-secrets}}]
          env:
            - {name: CLAS_IAM_HOST, value: clas-iam}
            - {name: CLAS_MERCHANT_HOST, value: clas-merchant}
            - {name: CLAS_CATALOG_HOST, value: clas-catalog}
            - {name: CLAS_ORDER_HOST, value: clas-order}
            - {name: CLAS_COMPAT_HOST, value: clas-compat}
          startupProbe: {httpGet: {path: /api/health, port: 8084}, periodSeconds: 10, timeoutSeconds: 3, failureThreshold: 30}
          readinessProbe: {httpGet: {path: /api/health, port: 8084}, periodSeconds: 10, timeoutSeconds: 3, failureThreshold: 6}
          livenessProbe: {httpGet: {path: /api/health, port: 8084}, periodSeconds: 20, timeoutSeconds: 3, failureThreshold: 3}
          resources: {requests: {cpu: 200m, memory: 384Mi}, limits: {cpu: 750m, memory: 768Mi}}
---
apiVersion: v1
kind: Service
metadata: {name: clas-compat, namespace: clas}
spec:
  type: ClusterIP
  selector: {app: clas-compat}
  ports: [{name: http, port: 8084, targetPort: 8084}]
