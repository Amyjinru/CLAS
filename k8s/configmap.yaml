apiVersion: v1
kind: ConfigMap
metadata:
  name: clas-config
  namespace: clas
data:
  MYSQL_HOST: mysql
  MYSQL_PORT: "3306"
  MYSQL_DATABASE: clas
  # 跑完 isolate-order-privileges.sql 后改为 clas_app，并增加 MYSQL_ORDER_USER: clas_order_app
  MYSQL_USER: clas
  REDIS_HOST: redis
  REDIS_PORT: "6379"
  CHAT_MODERATION_TIMEOUT_MS: "3000"
  CLAS_DEMO_ACCOUNTS_ENABLED: "true"
  CORS_ALLOWED_ORIGINS: "http://8.141.112.182"
