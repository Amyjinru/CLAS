# UC16 骑手配送测试报告

## 已执行自动化验证

| 类型 | 用例/命令 | 覆盖结果 |
| --- | --- | --- |
| 集成 | `RiderModuleIntegrationTest` | 骑手申请审核、真实并发领取互斥、容量上限、配送闭环、骑手—商家沟通、退款后临时佣金逆转、资料审核、打赏幂等、重复评价拒绝、超时扫描幂等、取餐前后取消、无关用户追踪拒绝、提现驳回/通过。2026-08-28 专项执行：14 项通过。 |
| 单元 | `RiderIdentityCryptoTest` | 身份证号 AES-GCM 加密/还原，以及默认脱敏展示。2026-08-28 专项执行：1 项通过。 |
| 集成 | `ModuleIntegrationTest` | 上线/开始接单、取餐后结束接单、订单主流程、鉴权与状态回归。 |
| 全量后端 | `mvn --batch-mode test` | 以最新运行结果为准；应记录 `surefire-reports` 的测试数、失败数和错误数。 |
| 前端构建 | `npm run build` | 骑手工作台及相关路由参与生产构建。 |

## 待补验收

1. 手动配送顺序、路线 ETA 降级和每日指标计算的专项自动化测试。
3. 一用户、一商家、一管理员、两骑手的浏览器端到端演示。
4. CI 成功链接、容器启动日志、日期化看板和统计截图。

## 通过标准

- 所有后端测试 `Failures=0` 且 `Errors=0`。
- 前端生产构建成功。
- 演示账户能完成“商家接单—骑手领取—取餐—送达—用户确认”的主链路。
- 任何越权访问、重复记账或非法状态转换均被拒绝且不写入错误数据。

## 本轮专项结果（2026-08-28）

执行命令：`mvn --batch-mode '-Dtest=RiderModuleIntegrationTest,RiderIdentityCryptoTest' test`。

结果：`Tests run: 15, Failures: 0, Errors: 0, Skipped: 0`。其中，`RiderModuleIntegrationTest` 的 14 项集成测试和 `RiderIdentityCryptoTest` 的 1 项单元测试均通过。两位骑手同时领取同一订单时仅一方成功；打赏接口以相同幂等键重放时仅保留一笔 `rider_tip` 和一笔 `TIP` 结算；超时扫描连续运行两次时仅生成一条 `OVERDUE` 异常和一条扣款结算；订单在取餐前可取消并释放骑手归属，取餐后取消会被拒绝并引导至售后流程；无关用户不能查询配送位置；提现驳回会恢复可提现余额，审批通过会保留审核记录。
