# 微服务优化报告（2026-09-01）

## 1. 本次目标

以“数据由所属服务管理、跨服务只能使用受控内部契约”为统一规范，收敛目录、订单和兼容服务对 IAM 数据的直接读取；同时保持既有 JWT、角色、单设备会话与账户限制行为。

## 2. 已完成优化

| 范围 | 统一后的实现 | 结果 |
| --- | --- | --- |
| 服务间鉴权 | `/internal/**` 统一要求 `X-CLAS-Service`、`X-CLAS-Internal-Key` | 未认证内部调用返回 `401 / INTERNAL_UNAUTHORIZED` |
| IAM 鉴权契约 | `POST /internal/iam/v1/auth/validate` 验证 Bearer Token | IAM 校验会话、已授权/兼容角色和账户限制状态；不返回 `sessionToken` |
| catalog / order / compat 外部请求鉴权 | 统一为 JWT 本地验签后调用 IAM 验证命令 | 鉴权拦截器不再直接读取 IAM Mapper、触达会话或查询处罚；IAM 成功验证后刷新会话活跃时间 |
| compat 跨域查询 | 公开统计、商家身份校验、骑手隐私通话手机号走 catalog / IAM 内部 API | 这些已迁移路径不再直接查询商品、商家或用户表 |
| 契约文档 | 团购路由、internal 认证头、Token 验证契约统一记录 | 文档与网关及代码一致 |
| 团购网关路由 | `/api/deals` 默认目录路由改为普通前缀 | 买券、核销、核销记录和团购订单的正则路由可优先进入 order |
| CI/CD 与部署 | CI 测试、构建并导入四服务 SHA 镜像；部署脚本渲染并应用微服务清单 | 服务变更触发发布；四服务和网关均通过 rollout 后才切换 Ingress |
| Kubernetes Secret | 部署输入新增 `CLAS_INTERNAL_API_KEY` | 内部请求不再因缺失共享密钥全部返回 `401` |

## 3. 测试证据

执行命令：

```powershell
cd services
mvn --batch-mode clean test
```

结果：**BUILD SUCCESS**，六个 Maven 模块均完成编译与测试。

| 测试类 | 覆盖内容 | 结果 |
| --- | --- | --- |
| `InternalServiceAuthInterceptorTest` | 正确服务凭据、错误密钥、缺少服务名 | 2 / 2 通过 |
| `AuthInterceptorTest`（catalog） | IAM 已验证身份接受、IAM 拒绝旧会话 | 2 / 2 通过 |

首次测试发现 JDK 24 与现有 Mockito/Byte Buddy 的动态 mock 组合不兼容；测试已改为真实 JWT 与轻量假 IAM 客户端，不再依赖动态字节码代理，随后全量测试通过。

本机没有 `services/scripts/env.local`，因此未启动依赖 MySQL 和 Redis 的四服务主路径冒烟测试。部署环境应补充执行 `services/scripts/smoke-main-path.ps1`，覆盖登录、目录浏览、下单、团购、配送与异常链路。

Kubernetes 验证说明：本机 `kubectl` 没有可用集群连接，客户端 dry-run 无法完成 API 发现；Docker Desktop 守护进程未启动，四个镜像也无法在本机完成构建验证。因此本次只完成代码构建、单元测试与清单人工静态检查，未将清单标注为“已部署”或“已验收”。

本轮统一测试（2026-09-01）：

| 命令 | 结果 |
| --- | --- |
| `mvn --batch-mode test`（`backend/`） | 通过，单体基线单元与集成测试全部通过 |
| `mvn --batch-mode clean test`（`services/`） | 通过，6 个 Reactor 模块全部通过 |
| `npm run test:build`（`frontend/`） | 34 个测试通过，生产构建成功 |
| `git diff --check` | 通过 |

本机没有可用的 Bash 运行时，无法执行 `scripts/k8s/deploy.sh` 的 `bash -n`；Docker 和 Kubernetes 集群同样不可用，因此镜像构建、Nginx 容器语法验证和真实集群 rollout 仍应由 CI/CD 与目标 k3s 环境完成。

## 4. 当前遗留风险

1. 四服务仍共享 `clas` 库和数据库账号，尚未具备数据库级最小权限隔离。
2. compat 的配送、派单、结算等仍直接读取订单/目录表；应优先以 `/internal/order/v1/**` 和 `/internal/catalog/v1/**` 补齐配送所需快照契约。
3. compat 的骑手申请、处罚、申诉和资料模块仍有 IAM Mapper 依赖；应迁移为 IAM 命令与查询接口。
4. 订单调用目录库存扣减仍为同步 HTTP，缺少 Outbox、持久化补偿任务与操作幂等号，服务在远端扣减后崩溃时存在不一致风险。
5. 内部调用目前使用共享密钥；生产环境应升级为每服务独立凭据、调用方白名单并优先使用 mTLS。

## 5. Kubernetes 就绪度

- 已新增：四服务独立镜像构建 Dockerfile、四个 Deployment/ClusterIP Service、catalog 上传 PVC、Nginx 网关与 Ingress 路由清单。
- CI/CD 已改为测试、构建并归档四服务镜像；部署脚本会创建内部密钥、运行迁移、等待四服务和网关健康，再切换 Ingress 并移除旧单体 Deployment。
- 未执行：真实集群 apply、镜像推送、Pod 健康检查与端到端验收；因此当前状态为**交付链路已就绪，尚未获得实际部署证据**。首次运行前须在 GitHub Secrets 配置 `CLAS_INTERNAL_API_KEY`。

## 6. 下一步建议

1. 为每个服务创建最小权限数据库账号，并在迁移期先限制写权限。
2. 先拆出订单配送快照/状态命令，再迁移 compat 的配送、结算和派单代码。
3. 为库存扣减和订单创建增加业务操作号、Outbox 与补偿任务。
4. 增加 IAM、catalog、order、compat 的 API 成功、鉴权失败、上游超时和补偿测试，并在 CI 中运行。
